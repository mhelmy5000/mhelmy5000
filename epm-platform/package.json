{
  "name": "mizan-epm",
  "version": "0.1.0",
  "private": true,
  "description": "Mizan EPM — Enterprise Performance Management platform (monorepo).",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:api": "node apps/api/dev-server.mjs"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.7.0"
}
